const d = 2;
const f = 5;
const target = 10;

// 
const numRollsToTarget = (d, f, target) => {
    let arr = [];

    if (d === 1 & f < target) return 0;
    if (target - f === f) return 1;

    for (let i = 1; i <= f; i++) {
        arr[i] - 
    }

};

console.log(numRollsToTarget(d, f, target))